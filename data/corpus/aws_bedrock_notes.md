# AWS Bedrock Notes (demo only)

Amazon Bedrock provides managed foundation models via an API. In production
systems, a gateway may route generate calls to Bedrock when credentials and
IAM permissions are available. This repository is an OSS learning demo and does
not represent any employer's production architecture. The bedrock provider here
is a stub that mocks responses unless AWS credentials are configured.
